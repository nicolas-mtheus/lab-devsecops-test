name: DevSecOps CI Gatekeeper

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  security-scan:
    name: Security Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2. Gitleaks: Usando imagem Docker oficial para pular a exigência de licença da Action
      - name: Gitleaks Scan
        run: |
          docker run --rm -v $(pwd):/path zricethezav/gitleaks:latest detect --source="/path" --verbose --redact

      # 2. Semgrep: SAST (Análise Estática) para Java e Angular
      - name: Semgrep SAST Scan
        run: |
          # Usamos 'p/default' que já engloba segurança e boas práticas
          docker run --rm -v $(pwd):/src returntocorp/semgrep semgrep \
          --config=p/java \
          --config=p/typescript \
          --config=p/security-audit \
          --errors-only

      - name: Trivy Dependency Scan
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: 'fs'
          scanners: 'vuln,config,secret'
          exit-code: '1' 
          severity: 'CRITICAL,HIGH'
          format: 'table'

      - name: Security Report Summary
        if: failure()
        run: |
          echo "### ❌ Falha de Segurança Detectada!" >> $GITHUB_STEP_SUMMARY
          echo "A pipeline foi bloqueada porque vulnerabilidades críticas foram encontradas no código do GEDAI." >> $GITHUB_STEP_SUMMARY